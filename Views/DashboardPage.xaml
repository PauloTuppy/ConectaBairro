<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ConectaBairro.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:ConectaBairro.ViewModels"
      x:Name="dashboardPage"
      mc:Ignorable="d"
      Background="#F0F4F8">

    <Page.DataContext>
        <vm:DashboardViewModel />
    </Page.DataContext>

    <Grid RowDefinitions="Auto,*,Auto">
        
        <!-- HERO SECTION -->
        <Border Grid.Row="0" CornerRadius="0,0,32,32">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0078D4" Offset="0.0"/>
                    <GradientStop Color="#00B4D8" Offset="0.5"/>
                    <GradientStop Color="#48CAE4" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            
            <StackPanel Padding="24,40,24,32" Spacing="20">
                
                <!-- Header with Avatar -->
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="🌍 Your Neighborhood Today" FontSize="28" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="📍 São Paulo, SP - Vila Mariana" FontSize="14" Foreground="#E3F2FD"/>
                    </StackPanel>
                    
                    <!-- Avatar -->
                    <Border Grid.Column="1" Width="50" Height="50" CornerRadius="25" Background="White" BorderBrush="#E3F2FD" BorderThickness="3">
                        <TextBlock Text="PS" FontSize="18" FontWeight="Bold" Foreground="#0078D4" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>

                <!-- Search Bar -->
                <Border Background="White" CornerRadius="16" Padding="4">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="20" VerticalAlignment="Center" Margin="12,0,8,0"/>
                        <TextBox Grid.Column="1" PlaceholderText="Search courses, programs, resources..." Background="Transparent" BorderThickness="0" VerticalAlignment="Center" FontSize="14"/>
                        <Border Grid.Column="2" Background="#0078D4" CornerRadius="12" Padding="12,8" Margin="4">
                            <TextBlock Text="🎯" FontSize="16"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Stats Cards -->
                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="12">
                    <Border Grid.Column="0" CornerRadius="16" Padding="12">
                        <Border.Background>
                            <SolidColorBrush Color="White" Opacity="0.25"/>
                        </Border.Background>
                        <StackPanel Spacing="4" HorizontalAlignment="Center">
                            <TextBlock Text="⭐" FontSize="24" HorizontalAlignment="Center"/>
                            <TextBlock Text="450 XP" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Level 5" FontSize="12" Foreground="White" Opacity="0.9" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="1" CornerRadius="16" Padding="12">
                        <Border.Background>
                            <SolidColorBrush Color="White" Opacity="0.25"/>
                        </Border.Background>
                        <StackPanel Spacing="4" HorizontalAlignment="Center">
                            <TextBlock Text="📚" FontSize="24" HorizontalAlignment="Center"/>
                            <TextBlock Text="10" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Courses" FontSize="12" Foreground="White" Opacity="0.9" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="2" CornerRadius="16" Padding="12">
                        <Border.Background>
                            <SolidColorBrush Color="White" Opacity="0.25"/>
                        </Border.Background>
                        <StackPanel Spacing="4" HorizontalAlignment="Center">
                            <TextBlock Text="🏆" FontSize="24" HorizontalAlignment="Center"/>
                            <TextBlock Text="2/6" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Badges" FontSize="12" Foreground="White" Opacity="0.9" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24" Padding="20,24,20,20">

                <!-- INTERACTIVE MAP -->
                <StackPanel Spacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Text="🗺️ Neighborhood Map" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A"/>
                        <Button Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToMap">
                            <TextBlock Text="See all →" FontSize="14" Foreground="#0078D4"/>
                        </Button>
                    </Grid>
                    
                    <Border Background="White" CornerRadius="20" Height="200" Padding="0">
                        <Grid>
                            <WebView2 x:Name="MapWebView" CornerRadius="20"/>
                            
                            <Border x:Name="MapLoadingOverlay" CornerRadius="20">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#E8F4FD" Offset="0.0"/>
                                        <GradientStop Color="#D1E8FF" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
                                    <ProgressRing IsActive="True" Width="32" Height="32"/>
                                    <TextBlock Text="Loading map..." FontSize="12" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Background="#00000060" CornerRadius="0,0,20,20" Padding="16" VerticalAlignment="Bottom">
                                <Grid ColumnDefinitions="*,Auto">
                                    <StackPanel>
                                        <TextBlock Text="📍 Vila Mariana, São Paulo" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="5 resources near you" Foreground="#E0E0E0" FontSize="12"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Click="NavigateToGoogleMap" CornerRadius="12" Padding="14,8" Background="White">
                                        <TextBlock Text="Explore" Foreground="#0078D4" FontSize="13" FontWeight="SemiBold"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- FILTERS / CATEGORIES -->
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Border Background="#0078D4" CornerRadius="20" Padding="16,10">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="8" Height="8" Fill="White"/>
                                <TextBlock Text="All" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="8" Height="8" Fill="#EF4444"/>
                                <TextBlock Text="Health" Foreground="#333333" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="8" Height="8" Fill="#3B82F6"/>
                                <TextBlock Text="Education" Foreground="#333333" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="8" Height="8" Fill="#F59E0B"/>
                                <TextBlock Text="Jobs" Foreground="#333333" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="8" Height="8" Fill="#8B5CF6"/>
                                <TextBlock Text="Social" Foreground="#333333" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- FEATURED OPPORTUNITIES -->
                <StackPanel Spacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Text="Featured Opportunities" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A"/>
                        <Button Grid.Column="1" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToOpportunities">
                            <TextBlock Text="See all" FontSize="14" Foreground="#0078D4"/>
                        </Button>
                    </Grid>

                    <!-- Card 1 -->
                    <Border Background="White" CornerRadius="20" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="60" Height="60" CornerRadius="16" Background="#E8F5E9">
                                <TextBlock Text="💼" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="Income Support Program" FontSize="16" FontWeight="Bold" Foreground="#1A1A1A"/>
                                <TextBlock Text="3 openings • $200/month" FontSize="13" Foreground="#666666"/>
                                <Border Background="#E8F5E9" CornerRadius="8" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Text="🟢 Open" FontSize="11" Foreground="#2E7D32"/>
                                </Border>
                            </StackPanel>
                            <Button Grid.Column="2" CornerRadius="12" Padding="14,10" VerticalAlignment="Center" Background="#107C10">
                                <TextBlock Text="View" Foreground="White" FontWeight="SemiBold"/>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Card 2 -->
                    <Border Background="White" CornerRadius="20" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="60" Height="60" CornerRadius="16" Background="#E3F2FD">
                                <TextBlock Text="📚" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="IT Training Course" FontSize="16" FontWeight="Bold" Foreground="#1A1A1A"/>
                                <TextBlock Text="120 spots • Free" FontSize="13" Foreground="#666666"/>
                                <Border Background="#E3F2FD" CornerRadius="8" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Text="🔵 15 days left" FontSize="11" Foreground="#1565C0"/>
                                </Border>
                            </StackPanel>
                            <Button Grid.Column="2" CornerRadius="12" Padding="14,10" VerticalAlignment="Center" Background="#0078D4">
                                <TextBlock Text="View" Foreground="White" FontWeight="SemiBold"/>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Card 3 -->
                    <Border Background="White" CornerRadius="20" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="60" Height="60" CornerRadius="16" Background="#FFF3E0">
                                <TextBlock Text="🎉" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="Community Event" FontSize="16" FontWeight="Bold" Foreground="#1A1A1A"/>
                                <TextBlock Text="Cleanup Day • Saturday 9am" FontSize="13" Foreground="#666666"/>
                                <Border Background="#FFF3E0" CornerRadius="8" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Text="🟠 23 confirmed" FontSize="11" Foreground="#E65100"/>
                                </Border>
                            </StackPanel>
                            <Button Grid.Column="2" CornerRadius="12" Padding="14,10" VerticalAlignment="Center" Background="#FF9800">
                                <TextBlock Text="View" Foreground="White" FontWeight="SemiBold"/>
                            </Button>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- COMMUNITY ALERTS -->
                <StackPanel Spacing="12">
                    <TextBlock Text="🔔 Community Alerts" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A"/>
                    
                    <!-- Critical Alert -->
                    <Border CornerRadius="16" Padding="16" BorderThickness="2" BorderBrush="#FFCDD2">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FFEBEE" Offset="0.0"/>
                                <GradientStop Color="#FFFFFF" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#FFCDD2">
                                <TextBlock Text="⚠️" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Heavy Rain Expected" FontSize="15" FontWeight="Bold" Foreground="#C62828"/>
                                <TextBlock Text="Main Street - Watch for flooding" FontSize="13" Foreground="#666666" TextWrapping="Wrap"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="Now" FontSize="12" Foreground="#999999" VerticalAlignment="Top"/>
                        </Grid>
                    </Border>

                    <!-- Info Alert -->
                    <Border Background="White" CornerRadius="16" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#E3F2FD">
                                <TextBlock Text="💧" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Scheduled Water Outage" FontSize="15" FontWeight="SemiBold" Foreground="#1A1A1A"/>
                                <TextBlock Text="Downtown area 2pm to 8pm" FontSize="13" Foreground="#666666"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="2h" FontSize="12" Foreground="#999999" VerticalAlignment="Top"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>

        <!-- BOTTOM NAVIGATION -->
        <Border Grid.Row="2" Background="White" CornerRadius="24,24,0,0" Padding="8,12,8,8">
            <Grid ColumnDefinitions="*,*,*,*,*,*" Height="56">
                <Button Grid.Column="0" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToHome">
                    <Border Background="#E3F2FD" CornerRadius="16" Padding="12,8">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock Text="🏠" FontSize="18"/>
                            <TextBlock Text="Home" FontSize="11" Foreground="#0078D4" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>
                
                <Button Grid.Column="1" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToCourses">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📚" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Courses" FontSize="9" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Button Grid.Column="2" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToForum">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="💬" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Forum" FontSize="9" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Button Grid.Column="3" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToMap">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📍" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Map" FontSize="9" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Button Grid.Column="4" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToBadges">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏆" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Badges" FontSize="9" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Button Grid.Column="5" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToProfile">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="👤" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Profile" FontSize="9" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
