<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ConectaBairro.Views.MapPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="#F0F4F8">

    <Grid RowDefinitions="Auto,*,Auto">
        
        <!-- HEADER -->
        <Border Grid.Row="0" Padding="20,40,20,20" CornerRadius="0,0,24,24">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#10B981" Offset="0.0"/>
                    <GradientStop Color="#059669" Offset="0.5"/>
                    <GradientStop Color="#047857" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel Spacing="8">
                <TextBlock Text="📍 Mapa de Recursos" FontSize="26" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Clique para abrir no Google Maps" FontSize="14" Foreground="#A7F3D0"/>
            </StackPanel>
        </Border>

        <!-- CONTEÚDO -->
        <ScrollViewer Grid.Row="1" Padding="16">
            <StackPanel Spacing="12">
                
                <!-- Botão Mapa Integrado -->
                <Button Click="OpenIntegratedMap_Click"
                        HorizontalAlignment="Stretch"
                        Padding="20"
                        CornerRadius="16">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#10B981" Offset="0.0"/>
                            <GradientStop Color="#059669" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Button.Background>
                    <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
                        <TextBlock Text="🗺️" FontSize="24"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Abrir Mapa Integrado" FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="Google Maps + OpenStreetMap" FontSize="12" Foreground="#A7F3D0"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <!-- Botão Mapa Externo -->
                <Button Content="🌐 Abrir no Google Maps" 
                        Click="OpenFullMap_Click"
                        HorizontalAlignment="Stretch"
                        Background="#F3F4F6"
                        Foreground="#374151"
                        FontSize="14"
                        Padding="12"
                        CornerRadius="12"/>

                <TextBlock Text="Buscar por Categoria:" FontSize="16" FontWeight="SemiBold" Foreground="#374151" Margin="0,12,0,4"/>

                <!-- Saúde -->
                <Button Click="OpenSaude_Click" HorizontalAlignment="Stretch" Padding="16" CornerRadius="12" Background="White">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="🏥" FontSize="28"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Saúde" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="UBS, Hospitais, UPAs" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <!-- Educação -->
                <Button Click="OpenEducacao_Click" HorizontalAlignment="Stretch" Padding="16" CornerRadius="12" Background="White">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="🎓" FontSize="28"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Educação" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="SENAI, SENAC, ETEC, IFSP" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <!-- Social -->
                <Button Click="OpenSocial_Click" HorizontalAlignment="Stretch" Padding="16" CornerRadius="12" Background="White">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="🏛️" FontSize="28"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Assistência Social" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="CRAS, CREAS, CadÚnico" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <!-- Trabalho -->
                <Button Click="OpenTrabalho_Click" HorizontalAlignment="Stretch" Padding="16" CornerRadius="12" Background="White">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="💼" FontSize="28"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Trabalho" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="SINE, CAT, Poupatempo" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <!-- Autonomia e Renda -->
                <Button Click="OpenAutonomia_Click" HorizontalAlignment="Stretch" Padding="16" CornerRadius="12" Background="#D1FAE5" BorderBrush="#10B981" BorderThickness="2">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="💰" FontSize="28"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Autonomia e Renda" FontSize="16" FontWeight="SemiBold" Foreground="#047857"/>
                            <TextBlock Text="autonomiaerenda.com.br" FontSize="12" Foreground="#059669"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <!-- CNCT MEC -->
                <Button Click="OpenCNCT_Click" HorizontalAlignment="Stretch" Padding="16" CornerRadius="12" Background="#DBEAFE" BorderBrush="#3B82F6" BorderThickness="2">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="📚" FontSize="28"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Cursos Técnicos (MEC)" FontSize="16" FontWeight="SemiBold" Foreground="#1E40AF"/>
                            <TextBlock Text="cnct.mec.gov.br" FontSize="12" Foreground="#3B82F6"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

            </StackPanel>
        </ScrollViewer>

        <!-- BOTTOM NAV -->
        <Border Grid.Row="2" Background="White" CornerRadius="24,24,0,0" Padding="8,12,8,8">
            <Grid ColumnDefinitions="*,*,*,*,*" Height="56">
                <Button Grid.Column="0" Click="NavigateToHome" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏠" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Home" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="1" Click="NavigateToCourses" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📚" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Cursos" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Background="Transparent" BorderThickness="0">
                    <Border Background="#D1FAE5" CornerRadius="16" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="📍" FontSize="20"/>
                            <TextBlock Text="Mapa" FontSize="12" Foreground="#047857" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>
                <Button Grid.Column="3" Click="NavigateToBadges" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏆" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Badges" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="4" Click="NavigateToProfile" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Perfil" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
