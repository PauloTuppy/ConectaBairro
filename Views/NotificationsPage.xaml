<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ConectaBairro.Views.NotificationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{StaticResource BackgroundBrush}">

    <Grid RowDefinitions="Auto,*">
        
        <!-- HEADER -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="20,40,20,20">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Button Grid.Column="0" Style="{StaticResource GhostButton}" Click="GoBack">
                    <TextBlock Text="← Voltar" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>
                </Button>
                <TextBlock Grid.Column="1" Text="🔔 Notificações" Style="{StaticResource HeadingMD}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Style="{StaticResource GhostButton}">
                    <Border Background="{StaticResource SecondaryLightBrush}" CornerRadius="{StaticResource RadiusSM}" Padding="10,6">
                        <TextBlock Text="Limpar" FontSize="12" Foreground="{StaticResource SecondaryBrush}" FontWeight="SemiBold"/>
                    </Border>
                </Button>
            </Grid>
        </Border>

        <!-- CONTEÚDO -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="16" Padding="16">

                <!-- Filtros -->
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Background="{StaticResource TextPrimaryBrush}" CornerRadius="{StaticResource RadiusFull}" Padding="14,8">
                            <TextBlock Text="Todas (8)" Foreground="White" FontWeight="SemiBold" FontSize="13"/>
                        </Border>
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="{StaticResource RadiusFull}" Padding="14,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Ellipse Width="8" Height="8" Fill="{StaticResource ErrorBrush}"/>
                                <TextBlock Text="Não lidas (3)" Style="{StaticResource BodySM}"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="{StaticResource RadiusFull}" Padding="14,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <TextBlock Text="Cursos" Style="{StaticResource BodySM}"/>
                        </Border>
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="{StaticResource RadiusFull}" Padding="14,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <TextBlock Text="Alertas" Style="{StaticResource BodySM}"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- HOJE -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Hoje" Style="{StaticResource HeadingSM}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    
                    <!-- Nova vaga -->
                    <Border CornerRadius="{StaticResource RadiusLG}" Padding="14" BorderThickness="2" BorderBrush="{StaticResource SecondaryLightBrush}" Background="{StaticResource SurfaceBrush}">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource SecondaryBrush}">
                                <TextBlock Text="📚" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="Nova vaga disponível!" Style="{StaticResource BodyMD}" FontWeight="Bold"/>
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource SecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Curso de Operador de Computador abriu 20 novas vagas" Style="{StaticResource BodySM}" TextWrapping="Wrap"/>
                                <TextBlock Text="Há 15 minutos" Style="{StaticResource Caption}"/>
                            </StackPanel>
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Padding="12,8" VerticalAlignment="Center">
                                <TextBlock Text="Ver" FontSize="12"/>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- XP ganho -->
                    <Border CornerRadius="{StaticResource RadiusLG}" Padding="14" BorderThickness="2" BorderBrush="{StaticResource PrimaryLightBrush}" Background="{StaticResource SurfaceBrush}">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource PrimaryBrush}">
                                <TextBlock Text="🏆" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="Parabéns! +50 XP" Style="{StaticResource BodyMD}" FontWeight="Bold"/>
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Você completou o módulo 'Introdução à Informática'" Style="{StaticResource BodySM}" TextWrapping="Wrap"/>
                                <TextBlock Text="Há 2 horas" Style="{StaticResource Caption}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Alerta -->
                    <Border CornerRadius="{StaticResource RadiusLG}" Padding="14" BorderThickness="2" BorderBrush="#FECACA" Background="{StaticResource SurfaceBrush}">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource ErrorBrush}">
                                <TextBlock Text="⚠️" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="Alerta Comunitário" Style="{StaticResource BodyMD}" FontWeight="Bold" Foreground="{StaticResource ErrorBrush}"/>
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource ErrorBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Chuva forte prevista para sua região nas próximas horas" Style="{StaticResource BodySM}" TextWrapping="Wrap"/>
                                <TextBlock Text="Há 3 horas" Style="{StaticResource Caption}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- ONTEM -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Ontem" Style="{StaticResource HeadingSM}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    
                    <Border Style="{StaticResource CardStyle}">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource BackgroundBrush}">
                                <TextBlock Text="📅" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Lembrete de inscrição" Style="{StaticResource BodyMD}" FontWeight="SemiBold"/>
                                <TextBlock Text="Faltam 5 dias para o fim das inscrições do PRONATEC" Style="{StaticResource BodySM}" TextWrapping="Wrap"/>
                                <TextBlock Text="Ontem às 14:30" Style="{StaticResource Caption}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource BackgroundBrush}">
                                <TextBlock Text="🎉" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Evento na comunidade" Style="{StaticResource BodyMD}" FontWeight="SemiBold"/>
                                <TextBlock Text="Mutirão de limpeza confirmado para sábado" Style="{StaticResource BodySM}" TextWrapping="Wrap"/>
                                <TextBlock Text="Ontem às 10:15" Style="{StaticResource Caption}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- ESTA SEMANA -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Esta Semana" Style="{StaticResource HeadingSM}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    
                    <Border Style="{StaticResource CardStyle}">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource BackgroundBrush}">
                                <TextBlock Text="💼" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Autonomia e Renda" Style="{StaticResource BodyMD}" FontWeight="SemiBold"/>
                                <TextBlock Text="Novas vagas abertas com bolsa de R$ 858/mês" Style="{StaticResource BodySM}" TextWrapping="Wrap"/>
                                <TextBlock Text="Segunda-feira" Style="{StaticResource Caption}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource BackgroundBrush}">
                                <TextBlock Text="🏅" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Badge desbloqueado!" Style="{StaticResource BodyMD}" FontWeight="SemiBold"/>
                                <TextBlock Text="Você conquistou 'Mestre do Tempo' por entregar tudo no prazo" Style="{StaticResource BodySM}" TextWrapping="Wrap"/>
                                <TextBlock Text="Terça-feira" Style="{StaticResource Caption}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>

                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
