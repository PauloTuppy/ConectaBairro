<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ConectaBairro.Views.ForumPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#F0F4F8">

    <Grid RowDefinitions="Auto,*,Auto,Auto">
        
        <!-- HEADER -->
        <Border Grid.Row="0" Padding="20,40,20,20" CornerRadius="0,0,24,24">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#6366F1" Offset="0.0"/>
                    <GradientStop Color="#8B5CF6" Offset="0.5"/>
                    <GradientStop Color="#A855F7" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Button Grid.Column="0" Click="Back_Click" Background="Transparent" BorderThickness="0" Padding="8">
                    <TextBlock Text="←" FontSize="24" Foreground="White"/>
                </Button>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                    <TextBlock Text="💬 Community Forum" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Ask questions, share knowledge" FontSize="12" Foreground="#E0E7FF"/>
                </StackPanel>
                <Button Grid.Column="2" Click="NewThread_Click" Background="White" CornerRadius="12" Padding="12,8">
                    <TextBlock Text="+ New" FontSize="14" Foreground="#6366F1" FontWeight="SemiBold"/>
                </Button>
            </Grid>
        </Border>

        <!-- FORUM CONTENT -->
        <ScrollViewer Grid.Row="1" Padding="16">
            <StackPanel Spacing="16">
                
                <!-- AI Assistant Card -->
                <Border Background="White" CornerRadius="20" Padding="16">
                    <Border.Shadow>
                        <ThemeShadow/>
                    </Border.Shadow>
                    <StackPanel Spacing="12">
                        <Grid ColumnDefinitions="Auto,*">
                            <Border Width="48" Height="48" CornerRadius="24" Background="#EEF2FF">
                                <TextBlock Text="🤖" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="AI Assistant" FontSize="16" FontWeight="Bold" Foreground="#1F2937"/>
                                <TextBlock Text="Get instant answers to your questions" FontSize="12" Foreground="#6B7280"/>
                            </StackPanel>
                        </Grid>
                        <Button Click="OpenAIChat_Click" HorizontalAlignment="Stretch" Background="#6366F1" CornerRadius="12" Padding="16">
                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                <TextBlock Text="💬" FontSize="18"/>
                                <TextBlock Text="Chat with AI" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Categories -->
                <TextBlock Text="📂 Categories" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                
                <!-- App Usage -->
                <Button Click="Category_AppUsage_Click" HorizontalAlignment="Stretch" Background="White" CornerRadius="16" Padding="16" BorderThickness="0">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <Border Width="50" Height="50" CornerRadius="12" Background="#DBEAFE">
                            <TextBlock Text="📱" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                            <TextBlock Text="App Usage" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="How to use features, tips &amp; tricks" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="24" FontSize="16" FontWeight="Bold" Foreground="#3B82F6"/>
                            <TextBlock Text="topics" FontSize="10" Foreground="#9CA3AF"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- Government Programs -->
                <Button Click="Category_Programs_Click" HorizontalAlignment="Stretch" Background="White" CornerRadius="16" Padding="16" BorderThickness="0">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <Border Width="50" Height="50" CornerRadius="12" Background="#D1FAE5">
                            <TextBlock Text="🏛️" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                            <TextBlock Text="Government Programs" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="Bolsa Família, PRONATEC, benefits" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="42" FontSize="16" FontWeight="Bold" Foreground="#10B981"/>
                            <TextBlock Text="topics" FontSize="10" Foreground="#9CA3AF"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- Courses -->
                <Button Click="Category_Courses_Click" HorizontalAlignment="Stretch" Background="White" CornerRadius="16" Padding="16" BorderThickness="0">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <Border Width="50" Height="50" CornerRadius="12" Background="#FEF3C7">
                            <TextBlock Text="📚" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                            <TextBlock Text="Courses &amp; Training" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="SENAI, SENAC, free courses" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="38" FontSize="16" FontWeight="Bold" Foreground="#F59E0B"/>
                            <TextBlock Text="topics" FontSize="10" Foreground="#9CA3AF"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- Jobs -->
                <Button Click="Category_Jobs_Click" HorizontalAlignment="Stretch" Background="White" CornerRadius="16" Padding="16" BorderThickness="0">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <Border Width="50" Height="50" CornerRadius="12" Background="#FCE7F3">
                            <TextBlock Text="💼" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                            <TextBlock Text="Jobs &amp; Employment" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                            <TextBlock Text="Job search, interviews, career" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="31" FontSize="16" FontWeight="Bold" Foreground="#EC4899"/>
                            <TextBlock Text="topics" FontSize="10" Foreground="#9CA3AF"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- Recent Threads -->
                <TextBlock Text="🔥 Recent Discussions" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,8,0,0"/>
                
                <!-- Thread 1 -->
                <Border Background="White" CornerRadius="12" Padding="14">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <Border Width="40" Height="40" CornerRadius="20" Background="#E0E7FF">
                            <TextBlock Text="MS" FontSize="14" FontWeight="Bold" Foreground="#4F46E5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                            <TextBlock Text="How to register for PRONATEC?" FontSize="14" FontWeight="SemiBold" Foreground="#1F2937" TextTrimming="CharacterEllipsis"/>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="Maria S." FontSize="11" Foreground="#6B7280"/>
                                <TextBlock Text="•" FontSize="11" Foreground="#D1D5DB"/>
                                <TextBlock Text="2h ago" FontSize="11" Foreground="#9CA3AF"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <Border Background="#DCFCE7" CornerRadius="8" Padding="6,2">
                                <TextBlock Text="🤖 AI" FontSize="10" Foreground="#166534"/>
                            </Border>
                            <TextBlock Text="5 replies" FontSize="10" Foreground="#9CA3AF" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Thread 2 -->
                <Border Background="White" CornerRadius="12" Padding="14">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <Border Width="40" Height="40" CornerRadius="20" Background="#FEF3C7">
                            <TextBlock Text="JC" FontSize="14" FontWeight="Bold" Foreground="#B45309" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                            <TextBlock Text="Best courses for IT beginners?" FontSize="14" FontWeight="SemiBold" Foreground="#1F2937" TextTrimming="CharacterEllipsis"/>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="João C." FontSize="11" Foreground="#6B7280"/>
                                <TextBlock Text="•" FontSize="11" Foreground="#D1D5DB"/>
                                <TextBlock Text="5h ago" FontSize="11" Foreground="#9CA3AF"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="12 replies" FontSize="10" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>

        <!-- BOTTOM NAV -->
        <Border Grid.Row="3" Background="White" CornerRadius="24,24,0,0" Padding="8,12,8,8">
            <Grid ColumnDefinitions="*,*,*,*,*" Height="56">
                <Button Grid.Column="0" Click="NavigateToHome" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏠" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Home" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="1" Click="NavigateToCourses" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📚" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Courses" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Background="Transparent" BorderThickness="0">
                    <Border Background="#EEF2FF" CornerRadius="16" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="💬" FontSize="20"/>
                            <TextBlock Text="Forum" FontSize="12" Foreground="#6366F1" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>
                <Button Grid.Column="3" Click="NavigateToBadges" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏆" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Badges" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="4" Click="NavigateToProfile" Background="Transparent" BorderThickness="0">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Profile" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
