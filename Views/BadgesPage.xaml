<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ConectaBairro.Views.BadgesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="#F0F4F8">

    <Grid RowDefinitions="Auto,*,Auto">
        
        <!-- HEADER -->
        <Border Grid.Row="0" CornerRadius="0,0,32,32">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#F59E0B" Offset="0.0"/>
                    <GradientStop Color="#D97706" Offset="0.5"/>
                    <GradientStop Color="#B45309" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            
            <StackPanel Padding="24,40,24,32" Spacing="20">
                <TextBlock Text="🏆 Your Achievements" FontSize="28" FontWeight="Bold" Foreground="White"/>
                
                <!-- Stats -->
                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="12">
                    <Border Grid.Column="0" CornerRadius="16" Padding="16">
                        <Border.Background>
                            <SolidColorBrush Color="White" Opacity="0.3"/>
                        </Border.Background>
                        <StackPanel Spacing="4" HorizontalAlignment="Center">
                            <TextBlock Text="⭐" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="450" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Total XP" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" CornerRadius="16" Padding="16">
                        <Border.Background>
                            <SolidColorBrush Color="White" Opacity="0.3"/>
                        </Border.Background>
                        <StackPanel Spacing="4" HorizontalAlignment="Center">
                            <TextBlock Text="🎖️" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="Level 5" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Explorer" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="2" CornerRadius="16" Padding="16">
                        <Border.Background>
                            <SolidColorBrush Color="White" Opacity="0.3"/>
                        </Border.Background>
                        <StackPanel Spacing="4" HorizontalAlignment="Center">
                            <TextBlock Text="🏆" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="2/6" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Badges" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Progress Bar -->
                <StackPanel Spacing="8">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Text="Progress to Level 6" FontSize="14" Foreground="White"/>
                        <TextBlock Grid.Column="1" Text="450/600 XP" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                    </Grid>
                    <Border CornerRadius="8" Height="12">
                        <Border.Background>
                            <SolidColorBrush Color="White" Opacity="0.3"/>
                        </Border.Background>
                        <Border Background="White" CornerRadius="8" Width="225" HorizontalAlignment="Left"/>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- CONTENT -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24" Padding="20">

                <!-- UNLOCKED BADGES -->
                <StackPanel Spacing="12">
                    <TextBlock Text="✅ Unlocked Achievements" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                    
                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="12">
                        <!-- Badge 1 -->
                        <Border Grid.Column="0" Background="White" CornerRadius="20" Padding="16">
                            <StackPanel Spacing="10" HorizontalAlignment="Center">
                                <Border Width="70" Height="70" CornerRadius="35" Background="#DCFCE7">
                                    <TextBlock Text="🚀" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="First Steps" FontSize="12" FontWeight="SemiBold" Foreground="#1F2937" TextAlignment="Center"/>
                                <TextBlock Text="+50 XP" FontSize="11" Foreground="#10B981" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Badge 2 -->
                        <Border Grid.Column="1" Background="White" CornerRadius="20" Padding="16">
                            <StackPanel Spacing="10" HorizontalAlignment="Center">
                                <Border Width="70" Height="70" CornerRadius="35" Background="#FEF3C7">
                                    <TextBlock Text="⏱️" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Time Master" FontSize="12" FontWeight="SemiBold" Foreground="#1F2937" TextAlignment="Center"/>
                                <TextBlock Text="+75 XP" FontSize="11" Foreground="#F59E0B" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Placeholder -->
                        <Border Grid.Column="2" Background="#F3F4F6" CornerRadius="20" Padding="16" BorderBrush="#E5E7EB" BorderThickness="2" Opacity="0.6">
                            <StackPanel Spacing="10" HorizontalAlignment="Center">
                                <Border Width="70" Height="70" CornerRadius="35" Background="#E5E7EB">
                                    <TextBlock Text="?" FontSize="36" Foreground="#9CA3AF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Coming soon..." FontSize="12" Foreground="#9CA3AF" TextAlignment="Center"/>
                                <TextBlock Text="" FontSize="11"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>

                <!-- LOCKED BADGES -->
                <StackPanel Spacing="12">
                    <TextBlock Text="🔒 Next Achievements" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                    
                    <!-- Locked Badge 1 -->
                    <Border Background="White" CornerRadius="16" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="60" Height="60" CornerRadius="30" Background="#F3F4F6">
                                <TextBlock Text="📚" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="Scholar" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="Complete 3 courses" FontSize="13" Foreground="#6B7280"/>
                                <Border Background="#F3F4F6" CornerRadius="6" Height="6" Margin="0,4,0,0">
                                    <Border Background="#3B82F6" CornerRadius="6" Width="40" HorizontalAlignment="Left"/>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="+100 XP" FontSize="13" Foreground="#3B82F6" FontWeight="Bold"/>
                                <TextBlock Text="1/3" FontSize="12" Foreground="#9CA3AF"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Locked Badge 2 -->
                    <Border Background="White" CornerRadius="16" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="60" Height="60" CornerRadius="30" Background="#F3F4F6">
                                <TextBlock Text="🤝" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="Collaborator" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="Help a colleague in the forum" FontSize="13" Foreground="#6B7280"/>
                                <Border Background="#F3F4F6" CornerRadius="6" Height="6" Margin="0,4,0,0">
                                    <Border Background="#10B981" CornerRadius="6" Width="0" HorizontalAlignment="Left"/>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="+80 XP" FontSize="13" Foreground="#10B981" FontWeight="Bold"/>
                                <TextBlock Text="0/1" FontSize="12" Foreground="#9CA3AF"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Locked Badge 3 -->
                    <Border Background="White" CornerRadius="16" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="60" Height="60" CornerRadius="30" Background="#F3F4F6">
                                <TextBlock Text="🌟" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="Perfect Score" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="Get maximum score on an assessment" FontSize="13" Foreground="#6B7280"/>
                                <Border Background="#F3F4F6" CornerRadius="6" Height="6" Margin="0,4,0,0">
                                    <Border Background="#8B5CF6" CornerRadius="6" Width="0" HorizontalAlignment="Left"/>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="+120 XP" FontSize="13" Foreground="#8B5CF6" FontWeight="Bold"/>
                                <TextBlock Text="0/1" FontSize="12" Foreground="#9CA3AF"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Locked Badge 4 -->
                    <Border Background="White" CornerRadius="16" Padding="16">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="60" Height="60" CornerRadius="30" Background="#F3F4F6">
                                <TextBlock Text="🔥" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="Persistent" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="Study for 7 consecutive days" FontSize="13" Foreground="#6B7280"/>
                                <Border Background="#F3F4F6" CornerRadius="6" Height="6" Margin="0,4,0,0">
                                    <Border Background="#EF4444" CornerRadius="6" Width="60" HorizontalAlignment="Left"/>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="+150 XP" FontSize="13" Foreground="#EF4444" FontWeight="Bold"/>
                                <TextBlock Text="3/7" FontSize="12" Foreground="#9CA3AF"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>

                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>

        <!-- BOTTOM NAV -->
        <Border Grid.Row="2" Background="White" CornerRadius="24,24,0,0" Padding="8,12,8,8">
            <Grid ColumnDefinitions="*,*,*,*,*" Height="56">
                <Button Grid.Column="0" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToHome">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏠" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Home" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="1" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToCourses">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📚" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Courses" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToMap">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📍" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Map" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="3" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToBadges">
                    <Border Background="#FEF3C7" CornerRadius="16" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="🏆" FontSize="20"/>
                            <TextBlock Text="Badges" FontSize="12" Foreground="#B45309" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>
                <Button Grid.Column="4" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToProfile">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Profile" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
