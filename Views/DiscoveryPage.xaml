<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ConectaBairro.Views.DiscoveryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="#F0F4F8">

    <Grid RowDefinitions="Auto,*,Auto">
        
        <!-- ============ HEADER ============ -->
        <Border Grid.Row="0" Padding="20,40,20,20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#6366F1" Offset="0.0"/>
                    <GradientStop Color="#8B5CF6" Offset="0.5"/>
                    <GradientStop Color="#A855F7" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            
            <StackPanel Spacing="16">
                <TextBlock Text="🔍 Descobrir" FontSize="32" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Explore oportunidades personalizadas para você" FontSize="14" Foreground="#E8E8FF" Opacity="0.9"/>
                
                <!-- Barra de Busca -->
                <Border Background="White" CornerRadius="16" Padding="4" Margin="0,8,0,0">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="18" VerticalAlignment="Center" Margin="12,0,8,0"/>
                        <TextBox Grid.Column="1" 
                                 PlaceholderText="Buscar cursos, programas, eventos..." 
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontSize="14"/>
                        <Border Grid.Column="2" Background="#6366F1" CornerRadius="12" Padding="12,8" Margin="4">
                            <TextBlock Text="⚡" FontSize="14"/>
                        </Border>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <!-- ============ CONTEÚDO ============ -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24" Padding="20">

                <!-- ============ CATEGORIAS ============ -->
                <StackPanel Spacing="12">
                    <TextBlock Text="📂 Categorias" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A"/>
                    
                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="12">
                        <!-- Cursos -->
                        <Border Grid.Row="0" Grid.Column="0" CornerRadius="20" Padding="20" Height="120">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#3B82F6" Offset="0.0"/>
                                    <GradientStop Color="#60A5FA" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="📚" FontSize="32"/>
                                <TextBlock Text="Cursos" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,8,0,0"/>
                                <TextBlock Text="10 disponíveis" FontSize="12" Foreground="#E0E7FF"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Programas -->
                        <Border Grid.Row="0" Grid.Column="1" CornerRadius="20" Padding="20" Height="120">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#10B981" Offset="0.0"/>
                                    <GradientStop Color="#34D399" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="🎯" FontSize="32"/>
                                <TextBlock Text="Programas" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,8,0,0"/>
                                <TextBlock Text="5 ativos" FontSize="12" Foreground="#D1FAE5"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Eventos -->
                        <Border Grid.Row="1" Grid.Column="0" CornerRadius="20" Padding="20" Height="120">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#F59E0B" Offset="0.0"/>
                                    <GradientStop Color="#FBBF24" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="🎉" FontSize="32"/>
                                <TextBlock Text="Eventos" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,8,0,0"/>
                                <TextBlock Text="3 esta semana" FontSize="12" Foreground="#FEF3C7"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Recursos -->
                        <Border Grid.Row="1" Grid.Column="1" CornerRadius="20" Padding="20" Height="120">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#EC4899" Offset="0.0"/>
                                    <GradientStop Color="#F472B6" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="📍" FontSize="32"/>
                                <TextBlock Text="Recursos" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,8,0,0"/>
                                <TextBlock Text="12 próximos" FontSize="12" Foreground="#FCE7F3"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>

                <!-- ============ RECOMENDADOS PARA VOCÊ ============ -->
                <StackPanel Spacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Text="⭐ Recomendados para Você" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A"/>
                        <Border Grid.Column="1" Background="#EEF2FF" CornerRadius="12" Padding="10,6">
                            <TextBlock Text="IA" FontSize="12" Foreground="#6366F1" FontWeight="Bold"/>
                        </Border>
                    </Grid>
                    
                    <!-- Card Recomendado 1 -->
                    <Border Background="White" CornerRadius="20" Padding="16">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="80" Height="80" CornerRadius="16">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#3B82F6" Offset="0.0"/>
                                        <GradientStop Color="#8B5CF6" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock Text="💻" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                                <TextBlock Text="Operador de Computador" FontSize="16" FontWeight="Bold" Foreground="#1A1A1A"/>
                                <TextBlock Text="SENAC • 3 meses • Gratuito" FontSize="13" Foreground="#666666"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Border Background="#DCFCE7" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="98% match" FontSize="11" Foreground="#166534" FontWeight="SemiBold"/>
                                    </Border>
                                    <Border Background="#FEF3C7" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="120 vagas" FontSize="11" Foreground="#92400E"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Card Recomendado 2 -->
                    <Border Background="White" CornerRadius="20" Padding="16">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="80" Height="80" CornerRadius="16">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#10B981" Offset="0.0"/>
                                        <GradientStop Color="#059669" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock Text="⚡" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                                <TextBlock Text="Técnico em Elétrica" FontSize="16" FontWeight="Bold" Foreground="#1A1A1A"/>
                                <TextBlock Text="SENAI • 6 meses • R$ 858/mês" FontSize="13" Foreground="#666666"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Border Background="#DCFCE7" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="92% match" FontSize="11" Foreground="#166534" FontWeight="SemiBold"/>
                                    </Border>
                                    <Border Background="#DBEAFE" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="45 vagas" FontSize="11" Foreground="#1E40AF"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- ============ TRENDING ============ -->
                <StackPanel Spacing="12">
                    <TextBlock Text="🔥 Em Alta na Comunidade" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A"/>
                    
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <!-- Trending 1 -->
                            <Border Width="160" CornerRadius="16" Padding="16">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FEF3C7" Offset="0.0"/>
                                        <GradientStop Color="#FFFFFF" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <StackPanel Spacing="8">
                                    <TextBlock Text="🍳" FontSize="32"/>
                                    <TextBlock Text="Confeiteiro" FontSize="14" FontWeight="Bold" Foreground="#1A1A1A"/>
                                    <TextBlock Text="25 interessados" FontSize="12" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Trending 2 -->
                            <Border Width="160" CornerRadius="16" Padding="16">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#DBEAFE" Offset="0.0"/>
                                        <GradientStop Color="#FFFFFF" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <StackPanel Spacing="8">
                                    <TextBlock Text="🏥" FontSize="32"/>
                                    <TextBlock Text="Cuidador" FontSize="14" FontWeight="Bold" Foreground="#1A1A1A"/>
                                    <TextBlock Text="40 interessados" FontSize="12" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Trending 3 -->
                            <Border Width="160" CornerRadius="16" Padding="16">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FCE7F3" Offset="0.0"/>
                                        <GradientStop Color="#FFFFFF" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <StackPanel Spacing="8">
                                    <TextBlock Text="📦" FontSize="32"/>
                                    <TextBlock Text="Logística" FontSize="14" FontWeight="Bold" Foreground="#1A1A1A"/>
                                    <TextBlock Text="18 interessados" FontSize="12" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>

                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>

        <!-- ============ BOTTOM NAV ============ -->
        <Border Grid.Row="2" Background="White" CornerRadius="24,24,0,0" Padding="8,12,8,8">
            <Grid ColumnDefinitions="*,*,*,*,*" Height="56">
                <Button Grid.Column="0" Style="{ThemeResource TextBlockButtonStyle}">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏠" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Home" FontSize="10" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="1" Style="{ThemeResource TextBlockButtonStyle}">
                    <Border Background="#EEF2FF" CornerRadius="16" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="🔍" FontSize="20"/>
                            <TextBlock Text="Descobrir" FontSize="12" Foreground="#6366F1" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>
                <Button Grid.Column="2" Style="{ThemeResource TextBlockButtonStyle}">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📍" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Mapa" FontSize="10" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="3" Style="{ThemeResource TextBlockButtonStyle}">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏆" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Badges" FontSize="10" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="4" Style="{ThemeResource TextBlockButtonStyle}">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Perfil" FontSize="10" Foreground="#888888" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
