<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ConectaBairro.Views.CoursesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="#F0F4F8">

    <Grid RowDefinitions="Auto,*,Auto">
        
        <!-- ============ HEADER ============ -->
        <Border Grid.Row="0" Padding="20,40,20,24" CornerRadius="0,0,28,28">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#3B82F6" Offset="0.0"/>
                    <GradientStop Color="#2563EB" Offset="0.5"/>
                    <GradientStop Color="#1D4ED8" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            
            <StackPanel Spacing="16">
                <TextBlock Text="📚 Catálogo de Cursos" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Encontre a oportunidade perfeita para você" FontSize="14" Foreground="#BFDBFE"/>
                
                <!-- Barra de Busca -->
                <Border Background="White" CornerRadius="14" Padding="4">
                    <Grid ColumnDefinitions="Auto,*">
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="18" VerticalAlignment="Center" Margin="12,0,8,0"/>
                        <TextBox Grid.Column="1" 
                                 PlaceholderText="Buscar cursos..." 
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontSize="14"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <!-- ============ CONTEÚDO ============ -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="20" Padding="20">

                <!-- ============ FILTROS ============ -->
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Border Background="#3B82F6" CornerRadius="20" Padding="16,10">
                            <TextBlock Text="Todos" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E5E7EB" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Ellipse Width="8" Height="8" Fill="#10B981"/>
                                <TextBlock Text="Com Bolsa" Foreground="#374151"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E5E7EB" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Ellipse Width="8" Height="8" Fill="#3B82F6"/>
                                <TextBlock Text="Gratuitos" Foreground="#374151"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E5E7EB" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Ellipse Width="8" Height="8" Fill="#F59E0B"/>
                                <TextBlock Text="Curta Duração" Foreground="#374151"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="20" Padding="16,10" BorderBrush="#E5E7EB" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Ellipse Width="8" Height="8" Fill="#EF4444"/>
                                <TextBlock Text="Próximos" Foreground="#374151"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- ============ DESTAQUES ============ -->
                <StackPanel Spacing="12">
                    <TextBlock Text="Recomendados para Você" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                    
                    <!-- Curso Destaque 1 -->
                    <Border Background="White" CornerRadius="20" Padding="16">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="80" Height="80" CornerRadius="16">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#10B981" Offset="0.0"/>
                                        <GradientStop Color="#059669" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock Text="⚡" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Spacing="6">
                                <TextBlock Text="Técnico em Manutenção Elétrica" FontSize="16" FontWeight="Bold" Foreground="#1F2937" TextWrapping="Wrap"/>
                                <TextBlock Text="SENAI • 6 meses" FontSize="13" Foreground="#6B7280"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Border Background="#DCFCE7" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="R$ 858/mês" FontSize="12" Foreground="#166534" FontWeight="SemiBold"/>
                                    </Border>
                                    <Border Background="#DBEAFE" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="45 vagas" FontSize="12" Foreground="#1E40AF"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Curso Destaque 2 -->
                    <Border Background="White" CornerRadius="20" Padding="16">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14">
                            <Border Grid.Column="0" Width="80" Height="80" CornerRadius="16">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#3B82F6" Offset="0.0"/>
                                        <GradientStop Color="#1D4ED8" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock Text="💻" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Spacing="6">
                                <TextBlock Text="Operador de Computador" FontSize="16" FontWeight="Bold" Foreground="#1F2937" TextWrapping="Wrap"/>
                                <TextBlock Text="SENAC • 3 meses" FontSize="13" Foreground="#6B7280"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Border Background="#F0FDF4" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="Gratuito" FontSize="12" Foreground="#166534" FontWeight="SemiBold"/>
                                    </Border>
                                    <Border Background="#FEF3C7" CornerRadius="8" Padding="8,4">
                                        <TextBlock Text="120 vagas" FontSize="12" Foreground="#92400E"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- ============ TODOS OS CURSOS ============ -->
                <StackPanel Spacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Text="📋 Todos os Cursos" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                        <TextBlock Grid.Column="1" Text="10 disponíveis" FontSize="14" Foreground="#6B7280"/>
                    </Grid>

                    <!-- Curso 1 -->
                    <Border Background="White" CornerRadius="16" Padding="14">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Width="50" Height="50" CornerRadius="12" Background="#FEE2E2">
                                <TextBlock Text="🏥" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Técnico em Enfermagem" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="SENAI • 12 meses • 50 vagas" FontSize="12" Foreground="#6B7280"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="→" FontSize="20" Foreground="#D1D5DB" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Curso 2 -->
                    <Border Background="White" CornerRadius="16" Padding="14">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Width="50" Height="50" CornerRadius="12" Background="#E0E7FF">
                                <TextBlock Text="🔧" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Soldador MIG/MAG" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="SENAI • 4 meses • R$ 750/mês" FontSize="12" Foreground="#6B7280"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="→" FontSize="20" Foreground="#D1D5DB" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Curso 3 -->
                    <Border Background="White" CornerRadius="16" Padding="14">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Width="50" Height="50" CornerRadius="12" Background="#FCE7F3">
                                <TextBlock Text="👴" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Cuidador de Idosos" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="SENAC • 3 meses • Gratuito" FontSize="12" Foreground="#6B7280"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="→" FontSize="20" Foreground="#D1D5DB" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Curso 4 -->
                    <Border Background="White" CornerRadius="16" Padding="14">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Width="50" Height="50" CornerRadius="12" Background="#FEF3C7">
                                <TextBlock Text="🍰" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Confeiteiro" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="SENAC • 4 meses • Gratuito" FontSize="12" Foreground="#6B7280"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="→" FontSize="20" Foreground="#D1D5DB" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Curso 5 -->
                    <Border Background="White" CornerRadius="16" Padding="14">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Width="50" Height="50" CornerRadius="12" Background="#DCFCE7">
                                <TextBlock Text="📊" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="Assistente Administrativo" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937"/>
                                <TextBlock Text="SESC • 5 meses • R$ 900/mês" FontSize="12" Foreground="#6B7280"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="→" FontSize="20" Foreground="#D1D5DB" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>

        <!-- ============ BOTTOM NAV ============ -->
        <Border Grid.Row="2" Background="White" CornerRadius="24,24,0,0" Padding="8,12,8,8">
            <Grid ColumnDefinitions="*,*,*,*,*" Height="56">
                <Button Grid.Column="0" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToHome">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏠" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Home" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="1" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToCourses">
                    <Border Background="#DBEAFE" CornerRadius="16" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="📚" FontSize="20"/>
                            <TextBlock Text="Cursos" FontSize="12" Foreground="#1D4ED8" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>
                <Button Grid.Column="2" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToMap">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="📍" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Mapa" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="3" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToBadges">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="🏆" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Badges" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="4" Style="{ThemeResource TextBlockButtonStyle}" Click="NavigateToProfile">
                    <StackPanel Spacing="2" HorizontalAlignment="Center">
                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Perfil" FontSize="10" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
